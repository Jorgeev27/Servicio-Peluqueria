/* La clase "Cortes" tiene propiedades para id, nombre, descripción, precio y tipo de corte. */
export class Cortes{
    public id_corte!: number;
    public nombre!: string;
    public descripcion!: string;
    public precio!: number;
    public id_tipocorte!: number;
}